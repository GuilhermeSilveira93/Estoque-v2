export enum RolesRequired {
  // eslint-disable-next-line no-unused-vars
  DESENV = 1,
  // eslint-disable-next-line no-unused-vars
  SUPERVISOR = 2,
  // eslint-disable-next-line no-unused-vars
  ADM = 3,
  // eslint-disable-next-line no-unused-vars
  CONSULTA = 4,
}
